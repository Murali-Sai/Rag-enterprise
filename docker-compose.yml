services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - chroma_data:/app/chroma_data
      - ./data:/app/data:ro
    restart: unless-stopped

volumes:
  chroma_data:
